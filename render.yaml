services:
  - type: web
    name: smart-forensic-api
    env: node
    plan: free
    healthCheckPath: /api/health
    buildCommand: npm install --include=dev && npm run build --workspace=@smart-forensic/shared && npm run build --workspace=@smart-forensic/security-engine && npm run prisma:generate --workspace=@smart-forensic/api && npm run build --workspace=@smart-forensic/api
    startCommand: node apps/api/dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: HOST
        value: 0.0.0.0
      - key: LOCAL_AGENT_MODE
        value: "false"
      - key: AGENT_OFFLINE_MS
        value: "15000"
      - key: JWT_SECRET
        generateValue: true
      - key: LOCAL_AGENT_TOKEN
        generateValue: true

  - type: web
    name: smart-forensic-dashboard
    env: node
    plan: free
    buildCommand: npm install --include=dev && npm run build --workspace=@smart-forensic/shared && npm run build --workspace=@smart-forensic/web
    startCommand: npm run start --workspace=@smart-forensic/web
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_API_URL
        fromService:
          type: web
          name: smart-forensic-api
          envVarKey: RENDER_EXTERNAL_URL
